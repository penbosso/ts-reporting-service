package com.example.tsreportingservice.model;

import org.springframework.data.jpa.repository.JpaRepository;

public interface TradeEngineActivityRepository extends JpaRepository<TradeEngineActivity, Long> {
}
